name: Generic CI
on:
  pull_request:
    paths-ignore:
      - 'auth/**'
      - 'orders/**'
      - 'payments/**'
      - 'tickets/**'

jobs:
  build:
    name: CI
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required" '
